# 2026-04-21 集成说明：529ee00 功能迁移到 a541952 骨架

## 结论

本次整合以远程 `origin/test@a541952` 的 `packages/` 与 `config/` 骨架为基线，手工迁入本地 `529ee00` 的比赛可视化与场景语义功能，没有回退到旧 `src/` 结构，也没有 force push。

当前已经完成并验证：

- `competition_rviz_tools` 迁入并可构建、启动。
- `TaskCommand.srv` 与 `SetObjectInteraction.srv` 迁入并可调用。
- `competition_rviz.launch.py` 迁入 `packages/bringup/dualarm_bringup/launch/`，可作为统一 RViz 入口。
- `config/competition/object_geometry.yaml`
- `config/competition/task_thresholds.yaml`
- `config/competition/workspace_profiles.yaml`
- `planning_scene_sync` 中的 `opened_split` / `dual_contact` 语义已在新结构下验证通过。
- `scene_model_pointcloud` 已能根据 managed scene 发布 `/competition/rviz/scene_model_points`。

## 迁移原则

- 保留远程新的仓库治理结构：`packages/`、`config/`、`docs/`。
- 只迁移与比赛闭环直接相关的功能逻辑，不整批迁入过程型记录文件。
- 对旧结构痕迹采用“按新结构重写 glue code”，不追求原 patch 原样重放。

## 本轮额外修补

除了功能迁移本身，这一轮还修了以下结构性问题：

- 将 `dualarm_task_manager` 和 `grasp_pose_generator` 的默认配置路径改为 `config/competition/workspace_profiles.yaml`。
- 将双臂底座位姿 launch/xacro 传参从旧的打包字符串改为 `left_base_x/y/z + roll/pitch/yaw`、`right_base_x/y/z + roll/pitch/yaw` 标量参数，避免 `xacro` 将负数误解析成 option。
- 将 bringup 中 `detector_model_path` 的默认值从个人本地路径 `/home/gwh/下载/best.3.pt` 改回安装后的 `detector` package share 路径。
- 修复 `competition_console_static_server.py` 仍按旧 `src/ops/competition_console_web` 找前端资源的问题，改为新 `packages/ops/competition_console_web`。
- 将 `competition_console_api_node.py` 的 repo root 自动发现条件改为 `packages + config`，不再依赖旧的 `STATE.md + packages` 组合。

## 运行验证证据

### 1. 构建验证

执行：

```bash
./build_workspace.sh --packages-up-to \
  dualarm_interfaces competition_rviz_tools dualarm_bringup planning_scene_sync \
  execution_adapter dualarm_task_manager grasp_pose_generator depth_handler tools \
  fairino_dualarm_description fairino_dualarm_moveit_config fairino_dualarm_planner \
  orbbec_gemini_bridge tf_node competition_console_api
```

结果：

- 相关包构建通过。

### 2. 接口与入口验证

执行：

```bash
ros2 pkg executables competition_rviz_tools
ros2 interface show dualarm_interfaces/srv/TaskCommand
ros2 interface show dualarm_interfaces/srv/SetObjectInteraction
ros2 launch dualarm_bringup competition_rviz.launch.py --show-args
```

结果：

- `rviz_task_bridge`
- `scene_interactive_markers`
- `grasp_debug_markers`
- `scene_model_pointcloud`
  四个可执行项存在。
- 两个新增服务接口可见。
- `competition_rviz.launch.py` 可正确展开参数。

### 3. 无硬件隔离域 smoke

执行模式：

- `ROS_DOMAIN_ID=91`
- `start_hardware:=false`
- `start_detector:=false`
- `start_table_surface_detector:=false`
- `start_camera_bridge:=false`
- `publish_fake_joint_states:=true`
- `rviz:=false`
- `dry_run:=true`
- `enable_action_bridge:=false`

结果：

- `competition_core + competition_rviz` 可启动。
- `/task/command`、`/scene/set_object_interaction` 存在。
- `/competition/rviz/scene_model_points`、`/planning/grasp_targets`、`/scene_fusion/scene_objects`、`/task_manager/events` 存在。

### 4. 物理语义 smoke

使用 `/usr/bin/python3` + `rclpy` 在隔离域内持续发布伪造的 `water_bottle` 与 `soccer_ball` 到 `/perception/scene_objects`，再调用：

- `opened_split` on `water_bottle_*`
- `dual_contact` on `soccer_ball_*`

实测结果：

- `opened_split response: True`
- `dual_contact response: True`
- managed scene 生命周期切换为：
  - 瓶子：`opened_split_active`
  - 球：`held_dual_contact`
- `/competition/rviz/scene_model_points` 收到点云：
  - `width = 900`
  - `frame = world`

## 当前剩余风险

- 真机链路尚未在本整合分支上做完整硬件验收。
- `competition_console_api` 的静态服务目录已修正并通过无缓冲 smoke 打印验证，但完整前后端联动未在本轮覆盖。
- 隔离域 smoke 结束时，`move_group` 在 SIGINT 退出阶段出现一次析构期段错误；这发生在关闭阶段，不影响本轮功能验证，但后续如要做长期值守，应单独排查 MoveIt 退出路径。

## 下一步建议

- 在该整合分支上继续做真实 RViz 展示与真机链路验收，不再回到旧 `src/` 分支追加功能。
- 若需要继续推进 console web，全量验证 `competition_integrated.launch.py start_console_web:=true`。
- 等真机最小闭环再通过后，再考虑合并到 `test`。
