# PUMA560 MATLAB Kinematics Reference

创建时间：2026-04-18
更新时间：2026-04-18

## 来源

- 源归档：
  - `/home/gwh/下载/MATLAB六轴机械臂仿真程序.rar`
- 原说明：
  - 安装机器人工具箱后运行 `puma560_arm.m`

## 为什么迁移

这组脚本对当前项目的价值在于：

- 提供一个完整的 6 轴机械臂 `FK/IK` 教学样例
- 展示如何把自写 `myFkine` / `myIkine` 与工具箱结果互相验证

## 使用边界

- 该样例的 DH/MDH 参数是 `PUMA560`，不是 `Fairino FR3`
- 不得直接把这里的参数、工作空间或解算结果套入当前 `dual-arm` 运行链
- 当前只能把它作为：
  - 推导思路参考
  - 单元验证结构参考
  - MATLAB 侧教学脚本参考

## 对当前项目的建议映射

- 若要为 `FR3` 写离线 FK/IK 验证脚本：
  - 保留这里的函数拆分方式
  - 将 MDH 参数替换成 `FR3`
  - 再与当前 MoveIt / planner / 真机回读结果做交叉验证
