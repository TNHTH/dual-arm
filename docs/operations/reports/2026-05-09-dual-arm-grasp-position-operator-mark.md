# 2026-05-09 双臂夹取位置现场标记

## 结论

2026-05-09 04:51:59 CST，用户现场确认：当前两个机械臂都在夹取位置上。

本记录是操作者现场标记，用于后续接续和保存示教点时定位当前状态；本次没有读取新的 ROS `/L/robot_state` 或 `/R/robot_state` 关节采样，也没有发送任何运动、Servo、PTP、MoveJ、MoveCart、程序运行或夹爪命令。

## 当前用途

- 该状态可作为“需要保存的双臂夹取示教位置”的现场标记。
- 后续若要让双臂自动回到该位置，必须分别读取并保存左右臂 6 轴关节角。
- 后续若要真正同步回到该位置，应使用 `/planning/plan_dual_joint` 和 `/execution/execute_trajectory synchronized=true`，不能只依赖两个控制器网页人工同时运行。

## 边界

- 这不是 verified joint preset。
- 这不是 MoveIt 规划目标。
- 这不是自动运动许可。
- 右臂先前 `motion_done=false` 问题按用户要求暂不处理；恢复前不要执行右臂自动运动测试。

## 建议下一步

1. 在左臂控制器或只读 ROS 状态中记录左臂当前 6 轴关节角。
2. 在右臂控制器或只读 ROS 状态中记录右臂当前 6 轴关节角。
3. 将两组关节角命名为同一组双臂夹取示教点，例如 `dual_grasp_pose_2026-05-09-0451`。
4. 先做 plan-only 验证，再考虑同步执行。
