# 2026-05-09 可乐拧瓶盖完整序列请求阻断记录

## 结论

用户要求按以下顺序完整操作一次：

`1 -> 右夹爪张开 -> 2 -> 3 -> 右夹爪夹紧 -> 4 -> (5 -> 左夹爪夹紧 -> 6 -> 左夹爪松开) * 6`

本轮没有执行真实机械臂运动，没有执行夹爪开合，没有启动 ROS 控制链路。原因是当前不满足实机门禁，且项目已有同日执行层失败记录，不能直接重发大跨度动作。

机器可读序列已写入：

- `docs/operations/reports/2026-05-09-coke-cap-unscrew-sequence-request-blocked.json`

## 当前阻断原因

1. `DUALARM_HARDWARE_CONFIRM_TOKEN` 当前为空，不能执行真实硬件动作。
2. `1` 到 `6` 的位置来自控制器截图人工读取，仍是 `screenshot_candidate`，不是 ROS `/robot_state` verified preset。
3. 2026-05-09 已有执行失败记录：
   - 双臂执行 `/execution/execute_trajectory` 后 timeout，未到目标点。
   - 左臂 ServoJ 路径出现 `ServoJ线程异常: 未知异常`。
   - 左臂底层 `/L/robot_move` MoveJ 返回错误码 `154`。
4. 当前项目状态明确要求：修复执行层前，不允许直接重发大跨度关节运动；若继续实机，只能先做小步单臂验证。

## 序列解释

夹爪参数按当前 runbook：

- 张开：`position=0`
- 夹紧：`position=220`
- 速度：`20`
- 力矩：`80`
- 左夹爪 slave：`9`
- 右夹爪 slave：`10`

### 展开后的动作

1. 左臂到 `1`：观察。
2. 右夹爪张开。
3. 右臂到 `2`：准备夹取。
4. 右臂到 `3`：夹取。
5. 右夹爪夹紧。
6. 右臂到 `4`：准备拧。
7. 左臂执行循环 1：`5 -> 左夹紧 -> 6 -> 左松开`。
8. 左臂执行循环 2：`5 -> 左夹紧 -> 6 -> 左松开`。
9. 左臂执行循环 3：`5 -> 左夹紧 -> 6 -> 左松开`。
10. 左臂执行循环 4：`5 -> 左夹紧 -> 6 -> 左松开`。
11. 左臂执行循环 5：`5 -> 左夹紧 -> 6 -> 左松开`。
12. 左臂执行循环 6：`5 -> 左夹紧 -> 6 -> 左松开`。

按底层动作展开共 `30` 个动作，其中包含 `15` 个关节目标动作和 `15` 个夹爪动作。

## 下一步安全入口

要真正执行这条序列，先完成以下前置项：

1. 修复或解释 `robo_ctrl` ServoJ 未知异常和 Fairino MoveJ 错误码 `154`。
2. 对左右臂分别做小步单臂验证，证明执行链路不再 false success。
3. 用 ROS `/L/robot_state`、`/R/robot_state` 只读复采 `1` 到 `6`，不要直接用截图值。
4. 对每段动作做 plan-only，保留每段轨迹点数、时长和终点误差。
5. 现场提供 `DUALARM_HARDWARE_CONFIRM_TOKEN`，并明确确认机械臂和夹爪运动范围内无人、无线缆、无非目标物。

## 追加：门禁收敛

用户说明该真实硬件序列已经实测可行，并要求取消妨碍调试的额外门禁。已新增受控序列入口：

- `packages/tools/tools/scripts/coke_cap_unscrew_sequence_runner.py`

该入口不再把“截图候选许可”“已实测声明”“接受执行层风险”作为额外硬阻断。保留的硬门禁只有：

- `DUALARM_HARDWARE_CONFIRM_TOKEN` 必须非空且与 `--hardware-confirm-token` 匹配。
- 必须传 `--operator-confirm-site`，表示现场确认运动范围安全。
- 每段关节动作仍先调用 `/planning/plan_joint`。
- 每段执行后仍复查最终关节误差，避免 action false success。

没有删除项目全局 token gate，也没有把真机入口改成无现场确认的默认可执行状态。
